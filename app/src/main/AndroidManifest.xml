<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fitwork">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fitwork"
        android:name="com.example.fitwork.ContextManager">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GoogleAuth">
        </activity>

        <activity android:name=".DashboardActivity">
        </activity>

        <activity android:name=".UserDetailsFirstActivity">
        </activity>

        <activity android:name=".UserDetailsSecondActivity">
        </activity>

        <activity android:name=".UserDetailsThirdActivity">
        </activity>

        <activity android:name=".UserDetailsActivity">
        </activity>

        <activity android:name=".CreateExerciseActivity">
        </activity>

        <activity android:name=".LineChartActivity">
        </activity>

        <activity android:name=".CreateWorkoutActivity">

        </activity>

        <activity android:name=".AddExerciseActivity">

        </activity>

        <activity android:name=".SearchUsersActivity">

        </activity>

    </application>

</manifest>